spring:
  datasource:
#   数据源基本配置
    username: root
    password: python
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test?useSSL=false&serverTimezone=GMT%2B8&allowPublicKeyRetreival=true
mybatis:
  mapperLocations: classpath:mapper/*.xml  typeAliasesPackage: com.example.demo.model